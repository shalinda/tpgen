import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { ReactiveFormsModule, FormsModule } from '@angular/forms';
import { ${className}sComponent } from './${lowerName}s.component';
import { ${className}sRoutingModule } from './${lowerName}s-routing.module';
import { OwlDateTimeModule, OwlNativeDateTimeModule } from 'ng-pick-datetime'
import { PumperSalesRoutingModule } from './pumpersales-routing.module';
import { TabsModule } from 'ngx-bootstrap/tabs';

@NgModule({
  imports: [
    CommonModule,
    FormsModule,
    ReactiveFormsModule,
    OwlDateTimeModule,
    OwlNativeDateTimeModule,
    PageHeaderModule,
    PaginationModule,
    PumperSalesRoutingModule,
    TabsModule,
    ${className}sRoutingModule
  ],
  declarations: [${className}sComponent],
  exports: [${className}sComponent]
})
export class ${className}sModule {}
